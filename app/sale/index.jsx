import { View, Text, SafeAreaView, ScrollView } from 'react-native'
import React from 'react'

const Sale = () => {
  return (
    <SafeAreaView>
      <ScrollView>
        <Text>Baby</Text>
      </ScrollView>
    </SafeAreaView>
  )
}

export default Sale